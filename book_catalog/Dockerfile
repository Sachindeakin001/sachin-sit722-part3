# Example Dockerfile for book-catalog
FROM python:3.10-slim
WORKDIR /book_catalog
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt
COPY app/ .
CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000"]
